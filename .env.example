# Для локального запуска Vite: адрес backend, по умолчанию http://localhost:8000
VITE_API_PROXY_TARGET=http://localhost:8000
# Папка исходных CSV (по умолчанию data/raw относительно корня проекта)
DATA_DIR=data/raw
